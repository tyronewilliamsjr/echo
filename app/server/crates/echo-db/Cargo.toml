[package]
name = "echo-db"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4.45"
sqlx = { version = "0.9", features = [
  "runtime-tokio",
  "tls-rustls",
  "postgres",
  "uuid",
  "chrono",
  "migrate"
] }
tokio = { version = "1", features = ["full"] }
uuid = "1.26.0"
